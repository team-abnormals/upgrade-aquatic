package com.teamabnormals.upgrade_aquatic.core.registry.other;

import net.minecraftforge.api.distmarker.Dist;
import net.minecraftforge.api.distmarker.OnlyIn;

@OnlyIn(Dist.CLIENT)
public class UARenderLayers {
	
	//private final RenderType TRANSPARENT = RenderType.cutoutMipped();
	//private final RenderType CUTOUT = RenderType.cutout();
	//private final RenderType TRANSLUSCENT = RenderType.translucent();
	
	public static void setBlockRenderLayers() {
		//RenderTypeLookup.setRenderLayer(UABlocks.BEACHGRASS, CUTOUT);
	}
	
}